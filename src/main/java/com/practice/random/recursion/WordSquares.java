package com.practice.random.recursion;

import java.util.List;

public class WordSquares {
//    https://leetcode.com/problems/word-squares/solution/
    public List<List<String>> wordSquares(String[] words) {
        // Sort words list first, for faster search

        //Loop through each words consider that as first word of word square

        //find squire by:
                // identify symetry by looping words[0].index[1] starting index of another word
                // word[0].index[2]+word[1].index[2] starting index of another word
                // keep looping to complete the squire
                // if cant complete then no squire and go to next word
        return null;
    }
}
